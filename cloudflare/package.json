{
  "name": "cadence-cloudflare",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "docker:dev": "docker build -t cadence-dev -f ../Dockerfile .. && docker run --rm -p 8000:8000 -v \"$(cd .. && pwd)/backend/data:/app/backend/data\" --env-file ../.env cadence-dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@cloudflare/containers": "*"
  }
}
